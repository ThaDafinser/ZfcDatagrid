<table>
    <thead>
        <tr>
            <?php foreach($this->columns as $column): ?>
                <td><?= $column->getLabel(); ?></td>
            <?php endforeach; ?>
        </tr>
    </thead>
    
    <tbody>
        <?php foreach($this->data as $row):?>
            <tr>
            <?php foreach($this->columns as $column): ?>
                <td><?= $row[$column->getUniqueId()]; ?></td>
            <?php endforeach; ?>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>
