<?php 
$url = $this->url(null, array(), array(), true);
if($this->overwriteUrl != ''){
    $url = $this->overwriteUrl;
}
?>
<?php foreach($this->exportRenderers as $type => $name): ?>
    <a class="btn" href="<?= $url.'?'.$this->generalParameterNames['rendererType'].'='.$type;?>">
        <?= $name; ?>
    </a>
<?php endforeach; ?>
